<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <title>Đơn Đặt Phòng Của Tôi</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="css/styles.css">
</head>
<body>
    <!-- Thanh điều hướng -->
    <nav class="navbar navbar-expand-lg navbar-light bg-white border-bottom">
        <div class="container">
                <a class="navbar-brand fw-bold" href="/">ZentoStay</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item"><a class="nav-link" href="/">Trang Chủ</a></li>
                    <li class="nav-item"><a class="nav-link" href="/room">Phòng</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Tiện Nghi</a></li>
                    <li class="nav-item"><a class="nav-link" href="/contact">Liên Hệ</a></li>
                    <li class="nav-item"><a class="nav-link" href="#">Giới Thiệu</a></li>
            </ul>
            <div class="d-flex align-items-center" id="auth-section">
            <!-- Viết script thay thế sử dụng firebase xác thực người dùng -->
            </div>
         </div>
        </div>
    </nav>
  <div class="container py-5">
    <h2 class="mb-4 fw-bold">ĐƠN ĐẶT PHÒNG</h2>
    <div class="row">
      <!--<% bookings.forEach((booking, index) => { %>
        <div class="col-md-4 mb-4">
          <div class="card shadow-sm h-100">
            <div class="card-body">
              <h5 class="card-title fw-bold"><%= booking.roomName %></h5>
              <p><%= booking.price.toLocaleString() %> vnd</p>
              <p><strong>Ngày Vào:</strong> <%= booking.checkin %></p>
              <p><strong>Ngày Trả:</strong> <%= booking.checkout %></p>
              <p><strong>Tổng:</strong> <%= booking.total.toLocaleString() %> vnd</p>
              <p><strong>ID Đơn:</strong> <%= booking.orderId %></p>
              <p><strong>Ngày Đặt:</strong> <%= booking.bookingDate %></p>
              
              <% if (booking.status === 'CANCELLED') { %>
                <span class="badge bg-danger">Đã Huỷ</span>
              <% } else if (booking.status === 'CONFIRMED') { %>
                <span class="badge bg-primary">Đã Xác Nhận Đặt Phòng</span>
              <% } else if (booking.status === 'PAID') { %>
                <span class="badge bg-success">Đã Thanh Toán</span>
                <div class="mt-2">
                  <a href="/review/<%= booking.orderId %>" class="btn btn-dark btn-sm">Đánh Giá</a>
                </div>
              <% } else { %>
                <span class="badge bg-warning text-dark">Đã Đặt</span>
                <div class="mt-2">
                  <a href="/booking/cancel/<%= booking.orderId %>" class="btn btn-danger btn-sm">Huỷ Đặt Phòng</a>
                </div>
              <% } %>
            </div>
          </div>
        </div>
      <% }) %>-->
    </div>
  </div>
</body>
</html>