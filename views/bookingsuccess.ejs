<!DOCTYPE html>
<html lang="vi">

<head>
  <meta charset="UTF-8">
  <title>Xác nhận đặt phòng</title>
  <style>
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 2rem;
      background-color: #fdfdfd;
      border-radius: 10px;
      box-shadow: 0 4px 16px rgba(0, 0, 0, 0.1);
      font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }

    .container h2 {
      font-size: 28px;
      margin-bottom: 1.5rem;
      color: #2c3e50;
      text-align: center;
      border-bottom: 2px solid #198754;
      padding-bottom: 0.5rem;
    }

    .container p {
      font-size: 18px;
      line-height: 1.6;
      color: #333;
    }

    .container p strong {
      color: #555;
      display: inline-block;
      width: 160px;
    }

    .badge.bg-success {
      background-color: #28a745 !important;
      font-size: 14px;
      padding: 6px 12px;
      border-radius: 20px;
      color: white;
    }
  </style>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="css/styles.css">
</head>

<body>
  <%- include('navbar') %>
  <div class="container mt-5">
    <h2>Xác nhận đặt phòng</h2>
    <p><strong>Tên khách:</strong>
      <%= booking.customer ? booking.customer.hoVaTen : 'Khách lạ' %>
    </p>
    <p><strong>Phòng:</strong>
      <%= booking.room ? booking.room.tenPhong : 'Không xác định' %>
    </p>
    <p><strong>Ngày nhận phòng:</strong>
      <%= booking.ngayNhanPhong.toLocaleDateString() %>
    </p>
    <p><strong>Ngày trả phòng:</strong>
      <%= booking.ngayTraPhong.toLocaleDateString() %>
    </p>
    <p><strong>Tổng tiền:</strong>
      <%= booking.tongTien.toLocaleString('vi-VN') %> VND
    </p>
    <p><strong>Trạng thái:</strong>
      <% if (booking.trangThai==='Chờ xác nhận' ) { %>
        <span class="badge bg-warning text-dark">Chờ xác nhận</span>
        <% } else if (booking.trangThai==='Đã xác nhận' ) { %>
          <span class="badge bg-success">Đã đặt phòng</span>
          <% } else if (booking.trangThai==='Đã hủy đơn' ) { %>
            <span class="badge bg-danger">Đã huỷ</span>
            <% } %>
    </p>
    <div class="mt-4 text-center">
      <a href="/" class="btn btn-outline-primary me-2">Về trang chủ</a>
    </div>
  </div>
</body>

</html>